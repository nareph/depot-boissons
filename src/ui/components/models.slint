export struct ProductUI { 
    id: string, 
    name: string, 
    stock: string, 
    price_offers: string, 
}

export struct LowStockProductUI { 
    name: string, 
    stock_info: string, 
}

export struct UserUI { 
    id: string, 
    name: string, 
    role: string, 
}